apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-deploy-demo.fullname" . }}
  labels:
    {{- include "helm-deploy-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-deploy-demo.selectorLabels" . | nindent 4 }}
